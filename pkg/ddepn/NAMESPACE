useDynLib(ddepn)
importFrom( genefilter, rowSds )
importFrom( gam, gam )
importFrom( gplots, heatmap.2 )
importFrom( gplots, colorpanel )
import( lattice )
import( coda )
import( igraph )
import( graph )
exportPattern("^[[:alpha:]]+")
